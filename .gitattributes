# Force LF on checkout everywhere so shell scripts, the Makefile and asm/C
# build correctly under WSL2/Linux even when the working copy lives on Windows.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.iso binary
